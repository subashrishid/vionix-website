---
layout: base.njk
pageTitle: "Our Services"
pageDescription: "Explore Vertaxes Technologies' comprehensive IT services: Web Development, Mobile Apps, CCTV, Server AMC, Networking, Cloud, Cybersecurity, and Hardware Repair."
activePage: "services"
permalink: /services/
---

<section class="page-banner">
    <h1>Our <span class="gradient-text">Services</span></h1>
    <p>Comprehensive IT solutions tailored to your business needs</p>
    <div class="breadcrumb"><a href="/">Home</a> <span>/</span> <span>Services</span></div>
</section>

<section style="padding:80px 0;background:var(--bg)">
    <div class="container">
        <div class="section-header" data-animate="fade-up">
            <span class="section-tag">What We Offer</span>
            <h2 class="section-title">End-to-End <span class="gradient-text">IT Services</span></h2>
            <p class="section-desc">From development to deployment, from installation to maintenance — we've got every aspect of your IT needs covered.</p>
        </div>

        {% for svc in services.services %}
        <div class="why-grid" style="margin-bottom:80px" data-animate="fade-up">
            {% if loop.index0 % 2 == 0 %}
            <div class="why-image"><img src="{{ svc.image }}" alt="{{ svc.name }}" loading="lazy"></div>
            <div class="why-content">
            {% else %}
            <div class="why-content" style="order:-1">
            {% endif %}
                <div class="service-icon" style="margin-bottom:16px"><i class="{{ svc.icon }}"></i></div>
                <h2 class="section-title">{{ svc.name | replace(" & ", " & <span class='gradient-text'>") | safe }}{% if " & " not in svc.name %}<span class="gradient-text"></span>{% endif %}</h2>
                <p style="color:var(--grey);margin-bottom:20px;line-height:1.8">{{ svc.desc }}</p>
                <div class="why-points" style="margin-bottom:24px">
                    {% for feat in svc.features %}
                    <div class="why-point">
                        <div class="why-point-icon"><i class="fas fa-check"></i></div>
                        <div><h4>{{ feat }}</h4></div>
                    </div>
                    {% endfor %}
                </div>
                {% if svc.name == "Server Setup & AMC" %}
                <a href="/server-amc/" class="btn btn-primary btn-glow"><i class="fas fa-headset"></i> Learn More</a>
                {% else %}
                <a href="/contact/" class="btn btn-primary btn-glow"><i class="fas fa-paper-plane"></i> Send Enquiry</a>
                {% endif %}
            </div>
            {% if loop.index0 % 2 != 0 %}
            <div class="why-image"><img src="{{ svc.image }}" alt="{{ svc.name }}" loading="lazy"></div>
            {% endif %}
        </div>
        {% endfor %}
    </div>
</section>

<section class="cta-section">
    <div class="cta-bg-anim"></div>
    <div class="container">
        <div class="cta-content" data-animate="fade-up">
            <h2>Need IT Support or Development Services?</h2>
            <p>Contact us today for a free consultation and let's discuss your requirements.</p>
            <div class="cta-buttons">
                <a href="{{ site.phoneLink }}" class="btn btn-white btn-glow"><i class="fas fa-phone"></i> Call Now</a>
                <a href="{{ site.whatsappLink }}" class="btn btn-whatsapp-alt btn-glow" target="_blank"><i class="fab fa-whatsapp"></i> WhatsApp Now</a>
                <a href="/contact/" class="btn btn-outline-white"><i class="fas fa-paper-plane"></i> Send Enquiry</a>
            </div>
        </div>
    </div>
</section>
